(comment) @keep
(groovy_doc) @keep

(groovy_package) @keep
(groovy_import) @keep

(class_definition
  (closure) @body) @signature

(declaration) @keep

(function_definition
  (closure) @body) @signature
(function_declaration) @keep
