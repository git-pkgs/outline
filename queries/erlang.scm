(comment) @keep

(module_attribute) @keep
(export_attribute) @keep
(export_type_attribute) @keep
(import_attribute) @keep
(behaviour_attribute) @keep
(record_decl) @keep
(type_alias) @keep
(spec) @keep
(callback) @keep
(wild_attribute) @keep

(fun_decl
  (function_clause
    body: (clause_body) @body)) @signature
